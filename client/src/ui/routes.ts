import {ROUTER_PREFIX} from 'src/config';

const ROUTES = {
  HOME: ROUTER_PREFIX + '/',
  USER: ROUTER_PREFIX + '/user',
  DASHBOARD: ROUTER_PREFIX + '/dashboard',

  CALENDAR: ROUTER_PREFIX + '/calendar',
  FORM: ROUTER_PREFIX + '/form',
  MAP: ROUTER_PREFIX + '/map',
  REPORT: ROUTER_PREFIX + '/report',
  RICH_TEXT_EDITOR: ROUTER_PREFIX + '/rich_text_editor',

  TABLE: ROUTER_PREFIX + '/table',
  SETTINGS: ROUTER_PREFIX + '/settings',
};

export default ROUTES;
