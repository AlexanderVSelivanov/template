import {withStyles} from '@material-ui/core';

import styles from './styles';
import layout from './layout';

export const PrivateLayoutWithStyle = withStyles(styles)(layout);

export default PrivateLayoutWithStyle;
